#ifndef FUNCTION
#define FUNCTION

#include <iostream>
#include <cstdlib>
#include <vector>

int qwe(std::string s1, std::string s2); //возвращает максимальное кол-во совпадений символов
double asd(int k, std::string s1, std::string s2); //возвращает коэффициент похожести двух слов
double asdf(std::string s, std::vector<std::string> arr); //возвращает коэффициент похожести слова и вектора

#endif // FUNCTION

